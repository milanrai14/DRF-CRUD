# Django REST Framework – Student CRUD API

This project is a simple **CRUD (Create, Read, Update, Delete) API** built using **Django REST Framework (DRF)**.  
It allows users to manage student records through RESTful API endpoints.

---

## 🚀 Features

- Create new student records  
- Retrieve all students  
- Retrieve a single student by ID  
- Update existing student data  
- Delete student records  
- Proper HTTP status code handling  
- Serializer-based validation  

---

## 🛠 Technologies Used

- Python  
- Django  
- Django REST Framework  
- SQLite (default Django database)

---


## 📂 Project Structure
